{
  "budget": {
    "max_epochs": 40,
    "max_wall_seconds": 21600
  },
  "data_dir": "./KuaiRand-Pure/data",
  "experiment_id": "E0015",
  "hypothesis": "The previous experiment yielded a GAUC of 0.43, indicating the predictions are inversely correlated with the ground truth. To isolate whether the BPR loss component is causing the inversion, we will disable BPR by setting bpr_weight to 0.0 and run a pure BCE model using the hybrid architecture.",
  "parameters": {
    "bce_weight": 1.0,
    "bpr_weight": 0.0,
    "embedding_dim": 64,
    "l2": 1e-05,
    "learning_rate": 0.001,
    "negatives_per_positive": 4,
    "patience": 5,
    "popularity_weight": 0.0
  },
  "schema_version": 1,
  "seed": 44,
  "template": "bpr_hybrid"
}
