{
  "budget": {
    "max_epochs": 40,
    "max_wall_seconds": 21600
  },
  "data_dir": "./KuaiRand-Pure/data",
  "evidence": "Dataset diagnostics identify zero-duration rows as a distinct subgroup with weak ranking performance.",
  "expected_effect": "Improve GAUC and nDCG@5 for missing-duration impressions without degrading the overall primary score.",
  "experiment_id": "E0020",
  "hypothesis": "Treating zero duration as an explicit missing category improves validation ranking.",
  "operator": "missing_duration_category",
  "parameters": {
    "bce_weight": 0.1,
    "bpr_weight": 1.0,
    "embedding_dim": 16,
    "l2": 1e-05,
    "learning_rate": 0.001,
    "negatives_per_positive": 4,
    "patience": 4,
    "popularity_weight": 0.0
  },
  "schema_version": 2,
  "seed": 0,
  "stage": "cleaning",
  "template": "bpr_hybrid"
}
